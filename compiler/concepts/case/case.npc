code 'case statement';

type
  Enum = (eiOne, eiTwo, eiThree, eiFour, eiFive);
  
var
  SomeEnum: Enum = eiThree;

SomeTestProc() {
  case 2 > 1 of
    if True: {
    };
    if False: {
    };
  end;
  //
  case SomeEnum of
    if .eiOne: {
    };
    if .eiTwo: {
    };
    if .eiThree: {
    };
    if .eiFour: {
    };
    if .eiFive: {
    };
  end;
};

end.